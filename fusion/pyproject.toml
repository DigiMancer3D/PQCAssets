[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rust_bulletproofs"
version = "0.1.0"
description = "Bulletproofs backend for FusionHash using dalek-bulletproofs"
requires-python = ">=3.8"
